[build]
  base = "salesfit"
  command = "npm install --legacy-peer-deps && npx expo export --platform web"
  publish = "salesfit/dist"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
